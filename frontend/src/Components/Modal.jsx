import { Flex } from '@chakra-ui/react'
import React from 'react'

const Modal = () => {
    return (
        <Flex w={'full'} h={'100vh'}>
            <Flex>

            </Flex>
        </Flex>
    )
}

export default Modal